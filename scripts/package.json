{
  "name": "sesh-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-off maintenance scripts for the sesh app (not part of the iOS build).",
  "scripts": {
    "reprocess-images": "node reprocess-images.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "sharp": "^0.33.4"
  }
}
