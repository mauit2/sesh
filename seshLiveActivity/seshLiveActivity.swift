// Intentionally empty.
//
// Xcode's "Widget Extension" template scaffolds a static home-screen
// widget here alongside the Live Activity. We don't ship one — BAC
// isn't a glanceable single-snapshot value, and the lock-screen
// Activity (in seshLiveActivityLiveActivity.swift) is what actually
// makes sense for this app. Leaving the file as a no-op rather than
// deleting it keeps the project file in sync with what Xcode
// generated; if you want to remove it from the target later, that's
// also fine.
